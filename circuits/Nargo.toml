[package]
name = "warptoad_v1"
type = "bin"
authors = ["Jim Jim Valkema"]

[dependencies]
# master, not a tag: the fix for modern nargo is unreleased (noir-lang/eddsa#18)
eddsa = { tag = "master", git = "https://github.com/noir-lang/eddsa" }
poseidon = { tag = "v0.3.0", git = "https://github.com/noir-lang/poseidon" }